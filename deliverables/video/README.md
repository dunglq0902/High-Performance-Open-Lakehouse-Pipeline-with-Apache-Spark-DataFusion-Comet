# Demo video output

The final reviewed recording and its generated provenance sidecar belong here:

- `spark-ui-comparison.mp4`
- `spark-ui-comparison.manifest.json`

Both generated files are intentionally ignored by Git because they must be produced from the final
clean-commit campaign evidence. Follow `docs/spark-ui-demo.md`; a diagnostic rehearsal is not a
submission artifact.
