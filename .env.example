# Copy to .env with `python scripts/bootstrap_env.py`.
# Values below are placeholders and are never used as defaults by Compose.
MINIO_ROOT_USER=replace-with-generated-access-key
MINIO_ROOT_PASSWORD=replace-with-generated-secret-key
ICEBERG_JDBC_PASSWORD=replace-with-generated-catalog-secret
AWS_REGION=us-east-1
